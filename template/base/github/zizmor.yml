# zizmor configuration (auto-discovered by zizmorcore/zizmor-action).
# SOURCE: docs/harness/README.md (workflow linting / CI-for-the-CI)
rules:
  cache-poisoning:
    # provenance.yml (ci-provenance module) is tag-triggered only and configures no dependency
    # cache, so there is no attacker-poisoned cache to taint the attested release artifact.
    ignore:
      - provenance.yml
